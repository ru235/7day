{
  "name": "service555",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:all": "node scripts/build-all.mjs",
    "deploy": "node scripts/deploy-ftp.mjs",
    "stats": "node scripts/report-stats.mjs",
    "detect": "npx impeccable detect packages/ apps/*/src/ || true"
  },
  "devDependencies": {
    "astro": "^5.18.2",
    "basic-ftp": "^6.2.1",
    "dotenv": "^17.4.2"
  }
}
